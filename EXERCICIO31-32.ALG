algoritmo "exercicio32"

var
  v : inteiro
inicio
      para v <- 1 até 10 faca
           se (v % 2 = 0) entao
              escreval("o numero ", v, " é par")
           senao
              escreval("o numero ", v, " é impar")
           fimse
      fimpara
fimalgoritmo
